# This script makes the necessary folders for the processing
# It takes one argument, the name of the upper folder (so that it can be changed) 


mkdir ${1}


mkdir ${1}/CBF_calc

mkdir ${1}/rawdata

mkdir ${1}/recon_results

mkdir ${1}/registration

mkdir ${1}/registration/rawdata

mkdir ${1}/ASL_results

mkdir ${1}/roast
